async def evaluate_response(
    question: dict, answer: str, resume_context: dict, job_role: str
) -> dict:
    pass


async def evaluate_full_interview(
    responses: list, resume_context: dict, job_role: str
) -> dict:
    pass
